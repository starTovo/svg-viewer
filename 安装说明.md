# SVG 查看器 App · 安装说明

## 一、把 APK 传到手机

产物位置：`E:\svg_view\android\out\svgview-aligned-signed.apk`

三种方式任选：

| 方式 | 操作 |
|------|------|
| **数据线（最稳）** | 手机连电脑 → 通知栏把「USB 用途」改成**传输文件** → 把 apk 拖进 `Download` 文件夹 |
| 微信 / QQ | 发给「文件传输助手」→ 手机上点开 → 用「其他应用打开」选择安装 |
| 网盘 | 上传后手机端下载 |

> ⚠️ 微信/QQ 直接点 apk 有时会被拦截，改到文件管理器里找到 `Download` 目录手动点开更稳妥。

---

## 二、允许安装未知来源应用

安卓 8+ 默认是**按应用授权**的，不是全局开关。

1. 打开手机 **设置**
2. 进入 **应用管理 / 应用与通知 / 应用**（不同品牌叫法不同）
3. 找到你准备**用来打开 apk 的那个应用**（文件管理器 / 浏览器 / 微信）
4. 点 **安装未知应用**（或「允许安装未知来源应用」）→ **打开**

常见品牌入口：

| 品牌 | 路径 |
|------|------|
| 小米 / Redmi | 设置 → 隐私保护 → 特殊权限设置 → 安装未知应用 |
| 华为 / 荣耀 | 设置 → 安全 → 更多安全设置 → 安装未知应用 |
| OPPO / 一加 / realme | 设置 → 权限与隐私 → 安装未知应用 |
| vivo | 设置 → 应用与权限 → 安装未知应用 |
| 三星 | 设置 → 生物识别和安全 → 安装未知应用 |

安装完成后**建议把这个开关关回去**。

---

## 三、安装

在文件管理器里点 `svgview-aligned-signed.apk` → 下一步 → 安装 → 完成。

首次安装若提示「有 play 保护墙检测」，点**详细信息 → 仍然安装**。这是自签名应用（不是从应用商店下载）的正常提示，不是病毒。

---

## 四、怎么用

### 打开 SVG 的三种方式

1. **App 内打开**：底部/顶部「打开」按钮 → 在文件选择器里挑 `.svg`
2. **从文件管理器直接打开**：长按 `.svg` 文件 → 「用其他应用打开」→ 选 **SVG 查看器**（App 已注册 .svg 关联）
3. **新建**：App 内置示例，直接改即可

### 主要功能

| 操作 | 手势 / 位置 |
|------|------------|
| 缩放 | 双指捏合（0.1x ~ 8x） |
| 平移 | 单指拖动 |
| 快速放大 | 双击画布（1x ↔ 2x 切换） |
| 复位 | 工具栏 1:1 / 适应屏幕 |
| 看透明背景 | 背景切换：棋盘格 / 纯白 / 纯黑 |
| 改代码 | 底部切到「代码」页，改完 300ms 自动重渲染 |
| 导出 PNG | 点悬浮按钮，2x 清晰度，存到相册/Download |
| 保存 SVG | 保存按钮，存到 Download |

### 代码改错了怎么办

写坏标签（比如漏了 `</svg>`）时，**画面不会变空白**——会保留上一次成功的渲染，同时在下方显示红色错误提示条告诉你哪里有问题。

**1.1.0 起带语法定位**：错误条会直接报出**行号和列号**，例如：

> 解析错误：结构不完整，请检查标签是否闭合 · 第 4 行第 3 列：闭合标签 `</circle>` 与 `<g>` 不匹配

**点一下这条错误提示，光标会自动跳到出错位置并选中它**，不用自己找。有多处错误时显示「发现 N 处问题 · i/N」，点「下一处」挨个跳。

能识别的错误：标签未闭合、闭合标签不匹配、属性值引号缺失或未闭合、孤立的 `<` 或 `>`、缺少 `<svg>` 根元素、重复 id。

### 一键清空与撤销

- **清空**：代码区工具行的「清空」一键清空全部源码。**不用怕**——清空前自动存档，之后点「撤销」或按 Ctrl+Z 即可完整恢复，画面不会白屏。
- **撤销 / 重做**：支持**多步**。连续打字按停顿合并成一步（不会一个字符一步）。
  - 撤销：工具行按钮，或 `Ctrl+Z` / `Cmd+Z`
  - 重做：工具行按钮，或 `Ctrl+Shift+Z` / `Ctrl+Y`
  - 上限 50 步；无可撤销内容时按钮置灰。

> 快捷键需要外接/蓝牙键盘；纯触屏就用工具行按钮。

---

## 五、常见问题

**Q：安装时提示「解析包时出现问题」**
A：多半是 apk 下载不完整。重新传一次，或对比电脑上文件大小。另外确认手机是安卓 8.0 以上（本 App 要求 minSdk 26）。

**Q：选文件的时候看不到我的 .svg**
A：文件选择器默认可能只筛图片。本 App 已放宽 MIME，但部分国产文件管理器仍会过滤。解决：在文件管理器里长按 .svg → 用其他应用打开 → 选本 App。

**Q：导出的 PNG 存在哪了**
A：相册 / `Pictures` 或 `Download` 目录，导出完会有提示文字告诉你具体位置。

**Q：关掉 App 后我改的内容还在吗**
A：在。App 会自动保存当前内容，下次打开自动恢复。

**Q：能卸载重装吗**
A：能，但重装会清空本地保存的内容。要保留就先「保存 SVG」导出一份。

---

## 六、技术信息（供排查）

- 包名：`com.svgview.app`
- **版本：1.2.0（versionCode 3）** —— App 内「设置 → 关于」显示的就是这个数（读自系统，不会和安装包的对不上）
- 最低系统：Android 8.0（API 26）
- 目标版本：API 34
- 签名：自签名（keystore 在 `E:\svg_view\android\_tools\svgview.keystore`）
- **后续升级请用同一个 keystore 签名**，否则无法覆盖安装

### 从旧版本升级

直接用新 APK 覆盖安装即可，**不需要卸载**，本地保存的内容不会丢（包名、签名、versionCode 都满足覆盖条件）。

---

## 七、自定义导出目录（1.2.0 起真正生效）

设置 → **导出位置** → 点 SVG 或 PNG 那一行的「修改」→ 弹出**系统文件夹选择器**，选一个文件夹即可，之后保存会直接写到那里。

- 选的是**文件夹**，不是单个文件
- 建议选允许写入的位置（如 `Download` 下新建的文件夹）。某些系统目录或第三方云盘目录可能不给写权限，此时 App 会**自动回退到下载目录并明确告诉你**，不静默失败、不丢文件
- 若在系统设置里清掉了本 App 的存储权限，目录授权会失效，重新选一次即可
- 不选就保持默认：下载目录 / 相册

## 八、双指缩放更稳了（1.2.0）

之前双指捏合时画面偶尔「抽搐」——根因是手势锚点在两根手指之间来回翻转，每次翻转画面会平移约一个指间距（实测单帧最大跳动 **335px**）。

1.2.0 重写了手势计算：手势开始时冻结基准，之后每帧纯解析求解，不再读被缩放过的元素尺寸；另加死区、方向滞回与轻度平滑。**实测单帧跳动降到 4.4px**（约等于手指本身抖动的底噪），反向跳变 0 次，双指轻点也不再误触发双击缩放。

### 重新打包

```bash
cd E:/svg_view/android
bash build.sh
```

改界面只需替换 `src/assets/index.html` 后重跑，**不需要动任何 Java 代码**。
（注意：不要改 `src/main/` 下的文件，那不是构建使用的路径。）
